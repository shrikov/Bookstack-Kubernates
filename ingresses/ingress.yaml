apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bookstack-ingress
  namespace: bookstack
spec:
  ingressClassName: nginx
  rules:
  - host: redhat-main.localdomain       #hostname
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: bookstack-service
            port:
              number: 8080
