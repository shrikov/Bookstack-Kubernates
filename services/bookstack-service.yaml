apiVersion: v1
kind: Service
metadata:
  name: bookstack-service
  namespace: bookstack
  labels:
    app: bookstack
spec:
  selector:
    app: bookstack
  ports:
  - port: 8080
    targetPort: 80
    protocol: TCP
    name: http
    nodePort: 30080
  type: NodePort
